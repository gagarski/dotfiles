WHEEL_RIGHT seek 10
WHEEL_LEFT  seek -10
WHEEL_UP    add volume -2
WHEEL_DOWN  add volume 2

Meta+w quit
Meta+ц quit


# Russian keyboard

х multiply speed 1/1.1                 # scale playback speed
ъ multiply speed 1.1
Х multiply speed 0.5
Ъ multiply speed 2.0
й quit
Й quit-watch-later
й {encode} quit 4
з cycle pause                          # toggle pause/playback mode
ю frame-step                           # advance one frame and pause
б frame-back-step                      # go back by one frame and pause
Ю playlist-next                        # skip to next file
Б playlist-prev                        # skip to previous file
Щ no-osd cycle-values osd-level 3 1    # cycle through OSD mode
щ show-progress
З show-progress
ш script-binding stats/display-stats
Ш script-binding stats/display-stats-toggle
я add sub-delay -0.1                   # subtract 100 ms delay from subs
Я add sub-delay +0.1                   # add
ч add sub-delay +0.1                   # same as previous binding (discouraged)
; add volume 2
ь cycle mute

в cycle deinterlace
к add sub-pos -1                       # move subtitles up
К add sub-pos +1                       #                down
е add sub-pos +1                       # same as previous binding (discouraged)
м cycle sub-visibility

М cycle sub-ass-vsfilter-aspect-compat
г cycle-values sub-ass-override "force" "no"
о cycle sub                            # cycle through subtitles
О cycle sub down                       # ...backwards
№ cycle audio                          # switch audio streams
Е cycle ontop                          # toggle video window ontop of other windows
а cycle fullscreen                     # toggle fullscreen
ы async screenshot                     # take a screenshot
Ы async screenshot video               # ...without subtitles
Ctrl+ы async screenshot window         # ...with subtitles and OSD, and scaled
Alt+ы screenshot each-frame            # automatically screenshot every frame
ц add panscan -0.1                     # zoom out with -panscan 0 -fs
Ц add panscan +0.1                     #      in
у add panscan +0.1                     # same as previous binding (discouraged)
Ф cycle-values video-aspect "16:9" "4:3" "2.35:1" "-1"
У cycle edition                        # next edition
д ab-loop                              # Set/clear A-B loop points
Д cycle-values loop-file "inf" "no"    # toggle infinite looping
ctrl+с quit 4
ctrl+р cycle-values hwdec "auto" "no"  # cycle hardware decoding

" add chapter 1                        #         next
